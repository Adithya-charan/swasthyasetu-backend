server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-1c0b1824-adityayaramalla-c6ac.k.aivencloud.com}:${DB_PORT:23377}/${DB_NAME:defaultdb}?useSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    username: ${DB_USER:avnadmin}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${APP_JWT_SECRET:9A72F67448CD9B1238914B924A22F263300C32345B73F2837341829D0A13FB9C}
    access-expiry-ms: 900000
    refresh-expiry-ms: 604800000

  storage:
    upload-dir: ./uploads